<UserControl x:Class="CinemaApp.Views.SeatPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CinemaApp.ViewModels"
             xmlns:conv="clr-namespace:CinemaApp.Converters"
             Background="{StaticResource PrimaryBrush}">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
        <conv:SeatColorConverter x:Key="SeatColor"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20,12,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Style="{StaticResource GhostButtonStyle}" Content="← Назад" Command="{Binding GoBackCommand}"/>
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="{Binding Session.Movie.Title}" Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center">
                    <Run Text="{Binding Session.StartTime, StringFormat=dd MMMM · HH:mm}"/>
                    <Run Text=" · "/>
                    <Run Text="{Binding Session.Hall.Name}"/>
                </TextBlock>
            </StackPanel>
        </Grid>

        <!-- Screen indicator -->
        <Border Grid.Row="1" Margin="40,20,40,4" Height="6" CornerRadius="3">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#E50914" Offset="0"/>
                    <GradientStop Color="#FF6B35" Offset="0.5"/>
                    <GradientStop Color="#E50914" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <TextBlock Grid.Row="1" Text="ЭКРАН" Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <!-- Seat grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center" Margin="20,12,20,12">
                <!-- Legend -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <Ellipse Width="14" Height="14" Fill="#2A3A5C"/>
                    <TextBlock Text="Свободно" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="6,0,16,0" VerticalAlignment="Center"/>
                    <Ellipse Width="14" Height="14" Fill="#E50914"/>
                    <TextBlock Text="Выбрано" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="6,0,16,0" VerticalAlignment="Center"/>
                    <Ellipse Width="14" Height="14" Fill="#3A3A4A"/>
                    <TextBlock Text="Занято" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Rows -->
                <ItemsControl ItemsSource="{Binding Rows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:SeatRowViewModel}">
                            <Grid Margin="0,2,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding RowLabel}" Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ItemsControl Grid.Column="1" ItemsSource="{Binding Seats}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:SeatViewModel}">
                                            <Button Width="26" Height="22" Margin="2,0"
                                                    Command="{Binding DataContext.ToggleSeatCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border CornerRadius="4" Width="26" Height="22"
                                                                Background="{Binding Converter={StaticResource SeatColor}}">
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Bottom summary bar -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectionSummary}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Foreground="{StaticResource AccentBrush}" FontSize="20" FontWeight="Bold">
                        <Run Text="{Binding TotalPrice, StringFormat=N0}"/><Run Text=" ₽"/>
                    </TextBlock>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource AccentButtonStyle}"
                        Content="Оплатить" Width="140"
                        Command="{Binding PurchaseCommand}"
                        IsEnabled="{Binding HasSelection}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
